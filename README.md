# Swift CookBook

A personal iOS recipe app built with SwiftUI.

## Features
- Add, edit, and delete recipes
- Attach photos to each recipe, with option to delete them
- Persistent storage using UserDefaults + JSON
